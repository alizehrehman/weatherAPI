const { formatWeatherData } = require('../server/helpers/formatWeatherData.js');

test('formatWeatherData helper throws an error when the rawData is not in the correct shape', () => {

})

test('formatWeatherData helper returns n/a for any data fields that does not contain data', () => {
    
})

test('formatWeatherData helper returns the formatted data in the correct shape', () => {
    
})