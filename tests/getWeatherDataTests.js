const { getWeatherData } = require('../helpers/getWeatherData.js');

test('getWeatherData helper gets data for a valid zipcode', () => {

})

test('getWeatherData helper returns an error for an invalid zipcode', () => {
    
})

test('getWeatherData helper returns the data in the correct shape', () => {
    
})